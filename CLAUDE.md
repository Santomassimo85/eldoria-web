## CRAFTING GIOCABILE = "L'Officina" in /officina (Gilda) · manuale in /crafting (2026-09-19; rifatto 2026-09-21)
- **PAGINE (2026-09-21)**: `/officina` (`src/pages/Officina.jsx` + `.css`, gruppo Gilda nel d20-menu, drawer e `NESSO_GROUP_OF`; tile Home e link Almanacco puntano qui) = hero + `<CraftingOfficina/>`; `/crafting` (`Crafting.jsx`) = SOLO manuale (7 pieghe: Come funziona · Rarità · Tiro · Tempo · Livelli · Professioni · Libero), CTA → /officina. La sezione `.cr-page` scopa entrambe (Officina.jsx ha `className="cine-page cr-page off-page"` così CraftingOfficina.css vale anche lì).
- **RARITÀ D&D (2026-09-21)** = `PREGIATURE` in crafting.js con chiavi NUOVE inglesi (= rarità Foundry): `scarso` (solo esito, 1–5) · `common` Comune 6–10 · `uncommon` Non comune 11–15 · `rare` Raro 16–20 · `veryRare` Molto raro 21–25 (grado Maestro) · `legendary` Leggendario 26+ (grado Leggenda). `LEGACY_TIER`/`normTier()` leggono le prove vecchie (comune→common, raro→uncommon, magico→rare, perfetto→veryRare): `tierByKey` (= `tierMeta` nell'Officina) normalizza sempre; `TIER_ORDER`, `tierByTotal`, `tierMinGrade` esportati da crafting.js. Costi 50/500/3.000/10.000/25.000 mo; tempi 5h/8h/5g/7g/14g (`CRAFT_BASE_MINUTES`); PE 10/25/60/150/300; Foundry `TIER_TO_FOUNDRY` common/uncommon/rare/veryRare/legendary 75/750/4.500/15.000/40.000.
- **SCELTA DELL'OGGETTO (2026-09-21)** = `src/data/craftingCatalogo.js`: `CATALOGO[prof]` = 6 "linee" (`{key, icon, common:T, uncommon:T, rare:T}`, oggetti D&D SRD: Spada Lunga → +1 → +2, Cotta di Maglia → di Mithral → +1, Pozione di Guarigione → Maggiore → Superiore…) e `LEGGENDARI[prof]` = 12 voci d12; crafting.js le fonde in `p.catalogo` e `p.creazioni.legendary`. Per le rarità `pick` (common/uncommon/rare) il giocatore sceglie la linea (`pickIdx`, CTA bloccata finché non sceglie) e l'esito è `pickLine[tierUscito]` (tiro basso = stessa linea alla rarità sotto; sotto 6 = d12 sulla tabella `scarso`); per veryRare/legendary (e per chi punta lì e tira basso) resta il d12 su `creazioni[tier]`. Voce del registro: `pick`, `pickName`, `d12` = 0 se scelto (UI e coda mostrano "oggetto scelto dal catalogo"). I nomi guidano `classifyCraftedItem` (nuove chiavi: martello dei, arco della, strumenti, liuto, mandolino, rampino, quadrell, LOOT per tomi/mappe/sigilli/costrutti…; "+N" nel nome = bonus).
- **TAVOLO DEL MASTER (2026-09-21)** = `MasterBoard` in cima all'Officina (solo Master, `onSnapshot` su `characters` condiviso con `MasterPanel`, tick 1 s del banco): per ogni artigiano stato "⚒ sta forgiando" (oggetto mirato/uscito, barra %, iniziato, pronto tra, ⏩ Termina ora) · "✓ finito, da ritirare" · "banco libero" + ultima creazione; contatori al lavoro/da ritirare/oggi e filtro "Solo chi sta lavorando". `MasterPanel` (professioni, PE, scorte) e `CraftLedger` restano sotto nella piega.
- **TEMPO DI LAVORO IN TEMPO REALE (2026-09-20)** = `src/data/craftingTime.js`: ogni prova ha minuti FISSI decisi prima del tiro (`craftMinutes`): base Comune 5h · Raro 8h · Magico 5 giorni · Perfetto 7 giorni (2026-09-20 sera); −2h con gli strumenti (senza = svantaggio, niente competenza); fino a 3 **componenti** (`COMPONENTS`, 10 oggetti SOLO descrittivi da far trovare in sessione, −30/45/60 min; scorta `crafting.components[key]` assegnata dal Master nel pannello artigiani, consumata nella transazione del tiro); **aiuto** artigiano −5% / mastro competente −10% del tempo (percentuale sul tempo dopo strumenti e componenti, +1 al tiro); Ritmo di bottega (lv 6) −¼; **ritmo** con calma ×2 (+2) / **di fretta ×½, −3 al tiro e 5% di fallimento critico** (`PACE_OPTIONS.critFail`: `rnd(100) <= 5` → voce `failed:true, skipped:true`, 0 PE, uso e componenti consumati, i materiali si considerano persi; stato "💥 fallito" nel registro); minimo 30 min. Il d20 non cambia il tempo. **Ogni componente dà +1d3 al tiro** (`COMPONENT_ROLL_DIE`, tirato nel `roll()`, `entry.compRolls[]` e `bonusParts.comps`, incluso in `entry.bonus`); **effetti dei componenti sull'oggetto** (`COMPONENTS[].effect`): Scheggia di Cristallo Grezzo +500 mo al `price` Foundry; Osso di Bestia Antica (bonus di attacco), Filo di Seta di Ragno (bonus al movimento), Erba Lunaria (potenziamento notturno) = nota "a scelta del DM" nella descrizione Foundry (`craftedItemToFoundryPayload({components})`) e `craft.componentEffects` → "⚠ DA DECIDERE" nella coda del Master. Il tiro scrive `entry.readyAt/minutes/work/components/help/pace/quality` + `crafting.busyUntil`; finché `readyAt` (ora server) non passa la pagina mostra solo la barra `.off-work` (tick 1 s), poi "📦 Ritira l'oggetto" (`claim`: risonda `serverNow`, fail-closed) rivela l'esito → invio in coda come prima (`craft.work/minutes/components/startedAt/readyAt`, nota "⏱ lavoro" nel form del Master, riga "Tempo di lavoro" nella descrizione Foundry). UN lavoro sul banco alla volta (la transazione rifiuta se c'è una voce senza inboxId/skipped). Master: "⏩ Termina ora" (readyAt = adesso) e scorte componenti; il pannello si ricarica all'apertura della piega. Vecchie voci senza `readyAt` = subito pronte. UI del banco: piastrelle `.off-tile` Strumenti (toggle) · Componenti (chip con ×n) · Aiuto · Materiali (vantaggio/svantaggio) · Ritmo (segmenti `.off-seg`) + riepilogo `.off-sum` con tempo scomposto e formula. MODS vecchi (tempo2/fretta/aiuto/strumenti0) sostituiti da help/pace/quality/tools. **Possesso verificato (2026-09-20 sera)** = `src/data/craftingOwnership.js`: strumenti della professione (`PROFESSION_TOOLS[key].aliases`, IT+EN, apostrofi normalizzati) e componenti (`COMPONENTS[].aliases`) si usano solo se risultano in `characters.inventory[]` (campo che la macro DM del Master esporta già: `src/foundry-macro-sync.txt` è la copia di riferimento SENZA apiKey/uidMap; ogni scheda va risincronizzata almeno una volta perché il campo compaia), in `actions[]`, nella scorta del Master (`crafting.components`, si consuma) o al Mercato (`items` isSold+buyerName). `toolsEvidence`/`componentEvidence` → tile Strumenti NON cliccabile: si accende da sola se risultano (✓ automatico), altrimenti "✗ non li possiedi" con l'hint del nome atteso (2026-09-20 sera; lo stato `tools` è stato tolto), chip componenti solo se `qty>0`; la transazione del tiro consuma la scorta se c'è, altrimenti esige la prova (`entry.componentProof[key]`, `toolsProof`) che finisce in `craft.componentProofs/toolsProof` nella coda del Master. Il Master bypassa tutto.
- **Registro dei craft del Master (2026-09-20)** = `CraftLedger` in fondo alla piega "Master · artigiani": totali del tavolo (a vita · oggi · settimana · in coda), una riga per PG (totale, oggi/1, sett./3, ultima prova), tocco = dettaglio raggruppato settimana (chiave `weekKey`) → giorno (`dayKey`) → voci con ora, oggetto, pregiatura (e mirata), tiro, PE, tempo, potenziatore, nota, stato. `crafting.totalCount` = contatore a vita incrementato nella transazione del tiro (il `log` tiene solo 40 voci): totale mostrato = max(totalCount, log.length). **Elimina prova (Master)**: ✕ su ogni voce del dettaglio → conferma inline → `deleteEntry` (transazione): via dal `log`, `totalCount`−1, `xp`−PE della voce, `weekCount`−1 se stessa settimana, `lastDayKey` azzerato se non restano prove quel giorno, `busyUntil` tolto se era sul banco, e `deleteDoc` del `foundry_inbox` se era in coda.
- **Prezzi FISSI dei materiali** (2026-09-20 sera, `PREGIATURA_COSTS[].mo/costo`): Comune 50 · Raro 500 · Magico 3.000 · Perfetto 10.000 mo, si pagano in gioco; valore Foundry dell'oggetto (`TIER_TO_FOUNDRY.price`) 20/75/750/4.500/15.000. Niente più range.
- **Manuale (2026-09-20)** riscritto corto in `Crafting.jsx`: Officina · 1 Come funziona (aperto) · 2 Pregiature (tabella tiro/tempo/PE/materiali da `PREGIATURA_COSTS`+`CRAFT_BASE_MINUTES`) · 3 Il tiro · 4 Il tempo di lavoro (+ i 10 componenti) · 5 Livelli e gradi (`XP_LEVELS`/`GRADE_BONUS`) · 6 Professioni · ★ Libero. Tolti recap, esempi di gioco (`ESEMPI_GIOCO`, `ESEMPI_INGREDIENTI`, `BONUS_LIVELLO_PG` restano in crafting.js ma non si mostrano). I numeri nel manuale vengono dalle costanti: non scriverli a mano.
- (SUPERATO 2026-09-21, vedi sopra: banco in /officina, manuale in /crafting.) Il banco è `src/pages/CraftingOfficina.jsx` + `.css`. L'Almanacco ha solo la "Sezione III · Il Crafting in breve" (link all'Officina). Stato sul PG: `characters/{uid}.crafting = { profession, xp, weekKey, weekCount, lastDayKey, log[≤40] }`.
- **Limiti**: 1 prova al giorno (giorno di calendario Roma) e 3 a settimana, reset **domenica 22:00** (`src/data/craftingWeek.js`: `craftWeekKey` = domenica d'inizio, `craftAllowance`). L'uso si consuma AL TIRO (transazione), non all'invio.
- **Prova**: pregiatura mirata (Comune/Raro/Magico; Perfetto solo dal grado Maestro) → d20 + mod caratteristica (`stats.str/dex/int/wis` come oggetti `{score,mod}`, MAG = max int/wis/cha) + competenza strumenti (toggle = bonus di competenza) + bonus di grado + condizioni del manuale (vantaggio/+2/+1/svantaggio/−2, dichiarate e scritte nella coda) → fascia 1–5/6–10/11–15/16–20/21+, MAI sopra la mirata; Artigiano: Scarso→Comune → d12 sulla colonna di `PROFESSIONI[].creazioni[tier]`. Oggetti "A o B / C" → scelta del giocatore (`itemChoices`).
- **Esperienza** (`src/data/craftingProgress.js`): PE per pregiatura 5/10/25/60/150 (nat 20 = doppi), 10 livelli (`XP_LEVELS`), grado = Sentiero del Maestro ogni 2 livelli (bonus +0/+1/+1/+2/+3 = `GRADE_BONUS`).
- **→ Foundry** (`src/data/craftingFoundry.js`): `craftedItemToFoundryPayload` produce lo STESSO payload del form del Master (tipo dnd5e da parole-chiave SRD: armi con danno/versatile/proprietà, armature con CA, consumabili, strumenti; rarità per pregiatura comune→uncommon→rare→legendary; prezzo/peso) + `origin:"crafting"`, `crafterUid`, `craft{…}`; scritto in `foundry_inbox` con target = inventario del giocatore. Nel form del Master la riga ha il chip "⚒ Officina" con la nota della prova. **Potenziatori** = `ENHANCERS` (lista BASE da far crescere): oggetti che il PG dichiara di possedere (checkbox), modificano il payload (danno extra elementale, +1 colpire, +1 CA, rarità +1…).
- **Master**: prove ILLIMITATE (chip ∞; `allow.unlimited`, la transazione salta il check). Piega "🎯 Master · artigiani" nell'Officina: cambia professione, ±25 PE, azzera usi, reset totale, **scorta potenziatori** (`crafting.enhancers[key]` con +/−). **Verifica possesso potenziatori** = `enhancerEvidence` in craftingFoundry.js: ok solo se scorta assegnata dal Master (si consuma 1 all'invio) · nome/alias in `characters.actions[]` (sync Foundry) · comprato al Mercato (`items` isSold + buyerName = nome PG); altrimenti chip 🔒 disabilitato; il Master bypassa. La prova finisce in `craft.enhancerProof` nella coda. **Una sola professione per PG**: conferma in 2 passi + transazione che rifiuta se già scritta. **Manuale a pieghe** (2026-09-19): in Crafting.jsx i capitoli sono `<details class="cr-fold">` chiusi, l'Officina resta aperta e in evidenza (`.cr-officina-sec`); `openFold(href)` apre la piega dai link/hash. `firestore.rules`: `foundry_inbox` create anche ai giocatori se `origin=="crafting"` e `crafterUid==targetUid==auth.uid` (deployate 2026-09-20). **Test (2026-09-20)**: in DEV `?vista=player` mostra al Master la pagina coi limiti di un giocatore (stesso doc `characters`); flusso verificato: scelta professione a 2 passi → tiro (contatori 0 oggi/2 settimana, PE e barra aggiornati) → ricarica (stato su Firestore, nulla si resetta) → invio in coda (chip ⚒ nel form del Master) → seconda prova bloccata "torna domani"; pannello Master: +25 PE fa salire livello/grado e il bonus in formula, Azzera usi, cambio professione, Reset totale. Gli hook di `preview` stanno PRIMA del `return` senza uid (altrimenti al logout sulla pagina React crasha). Limiti applicati lato client + transazione (regole `characters` = write aperto): non c'è difesa da chi scrive su Firestore a mano, come per il resto del sito. **Ora del SERVER, non del telefono** (`src/data/serverClock.js`, 2026-09-20): giorno/settimana si calcolano dall'intestazione HTTP `Date` di una HEAD al sito (Vercel/Vite la mandano); `clockOffset` corregge la pagina all'apertura e `roll()` rifà la sonda prima della transazione (`craftAllowance(cur, srvNow)`, `crafting.lastAt` = serverTimestamp). Se la sonda fallisce il tiro NON parte (fail-closed). Provato spostando `Date.now` di +8 giorni: resta "0 oggi". NON usare `getDocFromServer` per rileggere un serverTimestamp appena scritto: con l'onSnapshot attivo torna null.

## WORLD BOSS FIGHT = scena testo + sprite (2026-09-14, mappa tattica DISMESSA)
- `/world-boss-fight` → `src/pages/WorldBoss.jsx` (la versione "com'era": banner, scena boss sx / eroi dx, tab Status·Azioni·Log). `src/pages/tactics/*` (BossTactics/IsoBoard/BattleMapEditor/battleModel) CANCELLATA il 2026-09-15 (era già senza rotte); `DMG_TYPE_OPTIONS` vive in `worldBossSpells.js`. L'admin non ha più gittata/area/anteprima caselle. **Sprite dei PG (vivi + morti) e sfondi di battaglia (World Boss e Arena) si caricano da DM Admin → Sprite Personaggi (`/dm-admin/player-sprites`, `PlayerSpritesAdmin.jsx`)**: tenerla. Boss e minion (sprite, azioni, tipo di danno, forma ad area) stanno in DM Admin → World Boss.
- **Stat dei PG** (2026-09-15): i personaggi sincronizzati da Foundry hanno `stats.str/dex/con/int/wis/cha` come OGGETTI `{score, mod, save}` (costituzione = `con`, non `cos`) — mai renderizzarli/sommarli grezzi (React va in crash → pagina nera per il giocatore): usare `readStat/statMod/statSave` in WorldBoss.jsx. In DEV `?vista=player&pg=<uid>` carica la scheda di QUEL PG (azioni vere) per provare da giocatore.
- **Effetti a schermo (2026-09-15) = PIXEL in CSS** (`WorldBossVfx.jsx` + `WorldBossVfx.css`, portati da tactics/IsoBoard; le webp in public/animations NON si usano più). Ogni attacco scrive sul messaggio di chat `effect` (slash·arrow·bolt·aoe·heal·buff·shield·debuff) + `effectEl` (fire·frost·lightning·poison·darkness·radiant·arcane·physical) + `effectTargets`/`effectFrom` (chiavi `data-vfx-target`) + `effectMissTargets`/`effectKill`; li costruisce `fxFields()` in WorldBoss.jsx. Riconoscimento in `worldBossSpells.js`: `elementFor` (dmgType editor → "N dado <tipo> damage"/"danni da" → nome), `attackShapeFor`, `areaSpellFor` (aoeShape editor → nomi noti → "cono/cubo/sfera/linea di N piedi" + "each creature"; niente danno = non è area), `damageFormulaFor` (tabella SRD per le aree con damage "0"). **Magie ad AREA colpiscono TUTTI i nemici vivi** (`castAreaSpell`: un tiro di danno, ogni nemico tira il TS vs CD 8+comp+mod; boss/minion con `aoeShape` colpiscono tutti gli eroi vivi vs `spellDC`, default 13). In DEV dalla console `__wbVfx(kind, el, [targets], {from, miss, kill})` disegna un effetto senza scrivere su Firestore; nel tab Chrome dell'MCP (nascosto) le animazioni non girano: per vederle mettere in pausa con `animation-delay` negativo.
- **Abilità dei PG usabili (2026-09-17)**: la categoria "Abilità" della sync Foundry NON è più scartata in blocco: entrano solo le abilità che fanno danno / curano / danno PF temporanei / alzano la CA, riconosciute da `skillKindFor` in `worldBossSpells.js` (testo: campo `damage` → prima "NdM" della descrizione → tabella SRD; Foundry spesso TOGLIE i tiri inline dalla descrizione). Tipi: `rider` = dadi extra su un colpo d'arma (Divine Smite, Psionic Strike, Planar Warrior, Favored Foe → scelta dell'arma via `weaponPicker`, `handleActionRoll(arma, {rider})`), `attack` (Ram), `save` (TS del nemico vs CD 8+comp+mod: Wrath of the Storm, Gift of the Gem Dragon → `castAreaSpell(action, aoe, [bersaglio], skill)`), `area` (Breath Weapon, CD su COS via `dcAbility`), `heal` self/ally/allies (Second Wind, Lay on Hands = livello×5, Turn the Tide → `castSkillHeal`), `shield` = `stats.shield` (Form of Dread, Wild Shape → `castSkillShield`), `ac` → `castSelfBuff(action, bonus)`. `limited` (riposo/usi nel testo) = UNA volta per battaglia: `characters/{uid}.wbSkillUses[bossId][slug]`, bottone "usata". Sneak Attack resta automatico sul colpo del ladro (escluso dalla lista). Test rapido: `skillKindFor` sulle azioni reali (25 PG, 293 abilità → 13 usabili).
- **Quorum del turno eroi (2026-09-15)**: base = eroi ATTIVI (characters meno `hiddenPlayers`, 12 su 24 iscritti) e vivi; quando hanno agito ≥ 2/3 (`quorumTarget`, 8 su 12) il client dell'ultimo che agisce accorcia `expiryDate` a +10 min (transazione su `battle_meta/turn_tracker`, una volta per turno via `quorumTurn` = turnNumber, mai se mancano già meno di 10 min) e avvisa in chat; chip nel HUD (`.rpg-hud-quorum`). Regole: i giocatori possono scrivere anche `quorumTurn`.
- **Verso degli sprite**: campo `facing` su bosses/minions (`left` = disegno che guarda a sinistra → specchiato in scena, default; `right` = lasciato com'è, classe `.faces-right`), scelto in DM Admin col `FacingPicker` (anteprima identica alla scena). I minion evocati lo copiano dalla sagoma.
- `WorldBoss.css` riscritto nel Covo a token (Grenze/Alegreya/Cinzel, `--rpg-*` alias dei token del covo). La rotta è `theme-dark` + `covo-arena` (squame accese), la testata resta visibile (niente fullscreen).
- **Pannello Master (2026-09-15, tab Azioni)** = ATTACCO IN 3 PASSI + EROI: ① *Chi attacca* (chip boss/minion con barra HP, `attackerId`; "➕ Evoca minion" apre la riga della Caserma; sotto, cura/scudo/congeda dell'attaccante) → ② *Contro chi* (segmento "Tutti gli eroi vivi | Scelgo io" = `targetMode`, chip eroi multi-select su `selectedTargets`, riga riassunto "→ nomi") → ③ *Con cosa* (azioni dell'attaccante come `.rpg-act-btn` con icona+tag da `masterActionMeta`: ⚔ singolo/🌀 AREA/💚🛡⬆ su di sé/🌑 svantaggio; disabilitate senza bersaglio). `handleBossRoll(nemico, azione, targetIds)` riceve i bersagli del passo 2. Sezione **Eroi · cura & ferite**: righe compatte (solo eroi ATTIVI, link per gli inattivi), tocco = si apre `.rpg-hero-edit` con Ferisci −1/−3/−5/−10/A terra, Cura +1/+3/+5/+10/Del tutto, quantità a piacere, scudo (input, niente più `prompt`), ✕ buff CA, ⬆/⬇ prossimo tiro; `adjustHeroHp` tiene gli HP fra 0 e max. Su ≤639px i chip attaccanti scorrono in orizzontale.
- **Minion**: sagome in `minions` (Caserma dell'admin; in tendina TUTTE, le ⚡attive per prime) → il Master le EVOCA dal pannello Azioni (sezione "Minion in campo", sopra il Boss) in `world_boss_minions` (istanze vive: hp/maxHp/ac/shield/actions/sprite). Ogni minion ha i suoi attacchi (stessa `handleBossRoll`). I giocatori scelgono il BERSAGLIO (chip "Bersaglio" nel pannello o click sullo sprite; `currentTarget`, default primo vivo). VITTORIA solo quando boss + tutti i minion sono a 0 (`areAllEnemiesDead`); boss morto con servi vivi = banner "abbattete i suoi servi". Regole Firestore deployate (update giocatori limitato a hp/shield/nextTurnCondition/debuffSource su bosses e world_boss_minions). Sprite nemici SPECCHIATI via CSS (l'IA li disegna rivolti a sinistra, gli eroi stanno a destra; le keyframe includono scaleX(-1)). In DEV `?vista=player` mostra la pagina come un giocatore. Pannello bloccato = nota `.rpg-locked-note` + comandi attenuati, MAI opacità sul blocco intero (su fondo scuro diventa "nero").

## APP ANDROID = guscio Capacitor + push native (2026-09-15)
- `android/` (Capacitor 8, `capacitor.config.json` con `server.url` = sito Vercel): l'APK carica il sito LIVE, non va ricostruito per le modifiche web. Si ricostruisce solo se cambia `android/`/config (vedi `docs/android/README.md`: JDK 21, SDK in %LOCALAPPDATA%\Android\Sdk, `npm run android:apk`, alzare `versionCode`). APK pubblicato in `public/app/crit-happens.apk` (link in /notifications per chi è su browser Android). Keystore/google-services.json NON versionati (repo pubblico): backup a parte.
- Push: UN client (`src/push/pushClient.js`): web/PWA = FCM + service worker + VAPID; app = plugin `@capacitor/push-notifications`. Token in `characters/{uid}.fcmTokens` (stesso array), `sendPush` in functions manda `notification`+`android`+`webpush`; il SW template salta i messaggi con `notification` (niente doppioni). Interruttore per dispositivo in `/notifications` (`PushToggle.jsx`, localStorage `ch_push_pref`); `NotificationOptIn.jsx` rinnova il token al login, gestisce tasto Indietro Android e tocco sulla notifica (`ch-push-open`). index.html NON deve più disregistrare `firebase-messaging-sw.js`. **Un token = UN personaggio** (2026-09-18): `saveToken` toglie il token dagli altri doc `characters` che lo avevano (login con un altro account sullo stesso telefono → arrivavano le SUE notifiche, es. turni del torneo di Palar al master) e il logout lo rimuove dal personaggio che esce (`forgetPushToken`).
- NON toccare: iPhone/PC restano sito/PWA. Niente Google sign-in in WebView (il login è email/password, ok).

## TCG: 5 MAZZI SALVATI (2026-09-15)
- `characters/{uid}.tcgDecks = [{name, cards}]` (max `MAX_DECKS` = 5) + `tcgDeckIdx` (attivo). `tcgDeck` resta SEMPRE la copia del mazzo attivo (lobby/torneo/IA leggono `playableDeck(profile)` come prima). Profili vecchi: slot 1 = `tcgDeck` (virtuale finché non si salva). API in `src/tcg/collection.js`: `saveDeckSlot/setActiveDeck/renameDeckSlot/deleteDeckSlot` (rileggono il doc, scrivono decks+idx+tcgDeck). UI: riga "Mazzi salvati" in `DeckBuilder.jsx` (⭐ = attivo, Nuovo/Usa in partita/Rinomina/Elimina, ultimo slot non eliminabile). Il Manuale (`Manual.jsx`) ha la sezione "Perk delle Vie & kit delle Multiclassi" generata da `CLASS_PROGRESSION`/`MULTICLASS_PROGRESSION` (non duplicare i testi a mano) e spiega i 5 slot.

## FEATURE ATTIVA: Generatore Sessioni DM (2026-07-03)
- Piano completo + stato: `docs/generate-session/PLAN.md` e `docs/generate-session/progress.md`.
- Strumento privato (solo master + co-master) per generare/archiviare le prep-sessioni dei party AMEA/LEAF/ENOX con Claude Opus 4.8.
- Collezione `dm_sessions` (NON la `sessions` esistente = calendario). Isolamento per party sempre. `reference_sessions/*.html` = solo guscio grafico.

## TEMA LIVE: "R · Il Covo del Drago" (2026-09-08) — TUTTO il sito
- Mockup approvato `public/mockups/r-covo.html` → portato su tutto il sito in un colpo. Layer globale `src/styles/covo.css` (caricato PER ULTIMO in main.jsx, dopo nesso.css): token del covo (`--ossidiana/--roccia*/--osso*/--oro/--sangue` + respiro `--el/--el-2/--el-soft/--el-deep/--el-rgb`), rimappa i token Nesso (`--nx-*`) e legacy, testata, d20-menu, rubriche col filo a freccia, blocchi (bordo alto acceso + artigli al hover), CTA "lama", input incassati, tab = dadi d6 (::before), modali, tiro, occhio, responsive.
- **Respiri, fondo e particelle (2026-09-17)**: ogni respiro tinge anche il FONDO, non solo le squame — `html[data-soffio=fuoco]`/`:not([data-soffio])` caldo (#0d0a0a…), `gelo` blu-notte, `arcano` viola-notte, `veleno` verde-bosco: ogni blocco ridefinisce ossidiana/roccia-1..4 E i `-rgb` (trasparenze). Il blocco `html[data-soffio=bianco] body.theme-dark` usa i valori del Fuoco (tenerli allineati). `CovoOverlay.jsx`: `stile()` = braci (Fuoco) · cristalli (Gelo) · neve (Bianco, dischi soffici) · rune (Arcano, rombi pulsanti con punto d'ambra, nascono ovunque e salgono piano) · spore (Veleno, bolle con riflesso che ondeggiano); la SCIA resta fiamme/schegge (`cristalli()`). Bianco ammorbidito: squame appena accennate (`--pelle-1 #f5efe4 / --pelle-2 #e7dfce`), accento `--el #2f7aa6`, velo di carta dietro la prosa (`.home-lore-main, .nx-prosa, .ch-lore, .cine-prose`), citazione infernale rosso scuro, chip `.eroe-classe`/`.gl-seal`/`.nx-pillola` a inchiostro. Audit contrasto: nel browser scan JS (non usare rAF nel tab MCP nascosto: si blocca).
- **Respiro** (5 voci dal 2026-09-08 sera): Fuoco · Gelo · Arcano (viola) · Veleno (drago verde) · **Bianco = TEMA CHIARO** (`html[data-soffio="bianco"]` ribalta ossidiana/roccia/osso; sezione 15 di covo.css). **Le pagine di gioco (`body.theme-dark`: Arena, Bottega Arena, World Boss, TCG, Pet) restano SCURE anche col Bianco**: blocco `html[data-soffio="bianco"] body.theme-dark {…token scuri, respiro Fuoco…}` in covo.css (2026-09-17; prima combat e creazione PG in Arena diventavano chiari con scritte chiare). Il canvas delle squame legge i token dal body e osserva la sua classe, quindi si riscurisce da solo. I colori del covo nei CSS sono TOKEN (`var(--roccia-2)`, `rgba(var(--ossidiana-rgb),.x)`…) grazie a `node tools/covo-remap.mjs --tokenize` (idempotente; salta CovoOverlay): MAI riscrivere #221f27/#ece5d6 in chiaro, altrimenti il Bianco si rompe. Le squame del canvas leggono `--pelle-0/1/2`. Tastino Fuoco/Gelo nell'header (`.respiro` in App.jsx, localStorage `covo_soffio`, `html[data-soffio]`). **Admin/DM = respiro ARCANO (viola)** via `body.covo-admin` (rotte /dm-admin, /dm/, /agenti, /sessions/) — stesso tema, colori diversi.
- `CovoOverlay.jsx` (sostituisce NessoOverlay): canvas squame + braci/cristalli + squame accese al mouse; gesti globali delegati: l'IRIDE (`.gl-finestra-img`, `.covo-iride`) segue il cursore; il TIRO inietta `<span class="tiro">` su `.nx-pannello--tap, .cine-card, .ch-card, .deity-card, .gl-vetrata, .admin-card, .tacca, [data-tiro]` (20 = `.covo-crit`, 1 = `.covo-fumble`). Spento su `body.theme-dark`.
- Il menu resta `NessoNav` (stessi handler) ma l'Orbe è il D20 (svg + numero che rotola; sfaccettature = esagoni). GlacierHero: il varco è l'OCCHIO (`.gl-finestra-wrap` > `.gl-finestra` ellisse con palpebre ::before/::after, immagine = iride, `.gl-finestra-velo` = pupilla).
- Palette meccanica: `node tools/covo-remap.mjs` (idempotente; esclude Arena/TCG/WorldBoss/Pet): hex Nesso → Covo, viola/ciano → `var(--el*)`, magenta → oro, raggi 5–60px → 3px, Manrope → Alegreya. Nuovi CSS: usare i token del covo, MAI esadecimali del Nesso.
- Font: Grenze Gotisch (titoli, `--font-title/head/display`), Alegreya (`--font-ui/text`), Alegreya SC (`--font-sc`, etichette), Cinzel (`--font-num`, numeri/CTA). Link in index.html.
- **Tema chiaro "Alba del Nesso" DISMESSO**: `nesso-light*.css` non più importati (file e `tools/gen-light-theme.mjs` restano su disco), tastino ☀/☾ sostituito dal respiro. Pagine di gioco (`body.theme-dark`) ricevono solo i token/header.
- **Arena** (2026-09-08): stesso Covo, STRUTTURA INVARIATA (hub bento, le due card del fight nel palco, sottomenu — piacciono all'utente). `node tools/covo-remap.mjs --arena` ha rimappato Arena.css/ArenaHero/ArenaNessoViste/ArenaPalcoFight/ArenaBill (oro → `--el`, pietra → roccia, pergamena → osso); `src/pages/ArenaCovo.css` (ULTIMO import in Arena.jsx) toglie il fondale e mette i font; `body.covo-arena` (solo rotta /arena) accende CovoOverlay pur restando theme-dark. **Bottega Arena (2026-09-09) = Covo**: `ArenaMarket.css`/`ArenaMarketCatalogo.css` riscritti a token (body.covo-arena anche su /arena-bottega), struttura invariata. Hub Arena semplificato (ArenaBill.css LAYER 11: tabellone in una riga, 4 piastrelle + fila "Altro", classifica a blocco unico). TCG, World Boss restano com'erano.
- **Arena, IA delle Sfide Libere (2026-09-17)**: il guardiano in `Arena.jsx` (effetto dopo `aiTakeAction`) è a BATTITO: ogni 1,5 s + a ogni snapshot rilegge `arenaMetaRef` e programma iniziativa/azione dell'IA con chiavi `matchId:init` / `matchId:turn` in `aiInFlightRef` (entry `{t, at}`; appese > 25 s si liberano da sole; 400 ms di grazia dopo l'azione). I timer NON vanno cancellati negli aggiornamenti di arena_meta (era il bug: snapshot estranei azzeravano i timer lasciando il segnalino acceso → IA ferma, mai iniziativa). `DiceRoll.jsx`: un tiro che ne sostituisce un altro risolve la promessa precedente (`pendingResolve`), altrimenti l'await dell'IA restava appeso. Rete di sicurezza server: `functions/arena/botTurns.js` ora agisce anche sui match `kind fun` fermi da ≥ 90 s (`idleMs` da `turnExpiry`/`createdAt`, log "(automatico)", motore semplice armi-only), indipendente da fase torneo e pausa; i bot di riserva restano gated su `phase === combat`. Dopo modifiche: `npx firebase deploy --only functions:arenaBotTurns`.
- **Arena, effetti a schermo (2026-09-17) = gli stessi PIXEL del World Boss**: `Arena.jsx` monta `VfxLayer` di `WorldBossVfx.jsx` (il vecchio `ArenaVfxLayer`/`.avfx` in Arena.css è stato tolto). Nessun handler d'attacco tocca i VFX: il driver (useEffect su `arenaMeta.matches`) legge le righe di log `{pub, attId, defId, ts}` e `classifyArenaVfx(entry)` ricava `{kind, el, miss}` dal TESTO (cura/scudo/buff/debuff → forma; fuoco/gelo/fulmine/veleno/oscurità/sacro → elemento; "manca/nessun danno" → sbuffo mancato; bersaglio a 0 PF sull'ultima riga → schizzo finale). Ancore: `data-vfx-target="arena-fx:<matchId>:<uid>"` su `.cv-avatar-wrap`/`.cv-noavatar`; il proiettile parte dall'avatar di `attId`. Il campo parallelo `lastFx` (`withArenaFx(matches, matchId, effect, targetId, fromId)`) resta per pet/demoni/cure/IA: accetta codici legacy (`slash·ranged·fire·…·magic`) o `{kind, el}`. Per provare in DEV senza scrivere: `__wbVfx(kind, el, ["arena-fx:<matchId>:<uid>"], {from})` dalla console.
- **Arena, palette "TCG skin" scura (2026-09-17)**: Arena.css/ArenaHero.css definiscono su `.arena-page` le variabili chiare `--card` (osso 95%), `--cream`, `--paper`, `--amber-soft`, `--ink`/`--muted` scuri: in `ArenaCovo.css` (blocco "PALETTE TCG SKIN → SCURA") vengono ridefinite scure su `.arena-page`, `.arena-modal-dialog` e `.arena-drawer-dialog` (le modali sono in PORTAL fuori da .arena-page: scopare sempre anche loro) e ~40 superfici a colore fisso (scommesse, tabellone, statistiche classi, banner, turn-tracker, save-block, barre Forma Selvatica, input/select del Master, bottoni bianchi) sono ricoperte con la roccia. Strumento: `scan-light-css` (regole con background chiaro non coperte dagli strati successivi) + scan JS nel browser (luminanza > 170, alpha > .4) su hub/lizza/tabellone/libera/gesta/albo/regole/dadi/master/modali/combat/bottega.
- **Arena, superfici chiare residue (2026-09-17)**: ArenaHero.css conserva lo strato "Pergamena" con fondi osso al 55–95% (`.arena-page .action-groups > *`, `.item-grid > *`, `.target-picker`, `.arena-modal-dialog`, `.bracket-round-col`, `.bet-tournament-fighter`): nel combat i gruppi Trucchetti/Livello erano beige con testo chiaro. Coperti in `ArenaCovo.css` (blocco "SUPERFICI ANCORA PERGAMENA") con la roccia del Covo; i pezzi del loadout (class-select-btn, stat-assign-row, hp-roll-panel, fun-challenge-row, tb-modal) li ristilano già ArenaLizza/ArenaNessoViste. Per scovarne altre: nel combat live, scan JS delle superfici con luminanza > 170 e alpha > .4.
- **Arena "Entra in Lizza" (2026-09-09)**: per il MASTER apre la vista `master` = Registro del Lanista (`src/pages/ArenaLanista.css`, ultimo import): iscritti in cima, le due leve (Entra/Dai inizio) affiancate, Riserve·Ricompensa·Pericolo a PIEGHE `<details class="lan-piega">` (aperte desktop/chiuse mobile via `lanAperto`, pericolo sempre chiusa), strumenti a 3 chip; l'editor premi sta DENTRO il pannello master (niente più `.arena-reward-panel` gemello). Per i GIOCATORI = flusso `join` (`ArenaLizza.css`): stepper Classe→Caratteristiche→Punti Vita→Equipaggio, classi con icona+nome IT, su mobile armi/magie a piastrelle 2 colonne, footer Annulla|CTA col centro libero per il D20. Sul telefono `arena-page--lizza/--lanista` nascondono la testata Arena e rendono statico il tastino Hub.
- Scheda PG: CSS inline in SchedaPG.jsx già rimappato al covo. Scriba: l'overlay nero all'apertura è l'intro-video (feature esistente), non un bug del tema.

## Scala di superfici Nesso (2026-09-07)
- Token in `nesso.css` §0: `--nx-s1` (contenitori/input incassati) → `--nx-s2` (card) → `--nx-s3` (elevati: hover, tab attivi, modali, card DENTRO card) → `--nx-s4` (livello massimo). Deriva prugna, non più indaco piatto. Bordo "filo di luce" `--nx-hair` + riflesso `--nx-hair-hi`. Testo: `--nx-ink` / `--nx-body` / `--nx-muted` / `--nx-faint`.
- Regola: un elemento annidato SALE di un gradino, mai lo stesso colore del contenitore. Il gradiente ciano→viola è solo per CTA e chip attive; i tab (`.dmt-tab`, `.mkadm-tab`, `.geoadm-tab`) sono binario S1 + segmento S4 con filo ciano (nesso.css §12).
- Nuovi CSS: usare i token, non esadecimali. `node tools/nesso-surfaces.mjs` rimappa i vecchi grigi-indaco hardcoded (idempotente, esclude pagine di gioco); poi `node tools/gen-light-theme.mjs`.

## Regole restyle (sempre valide)
- Ricolorare = fallire. Ogni pagina cambia STRUTTURA, non solo colori.
- NON toccare logica/link/route/href. Solo markup + CSS/animazioni.
- NON toccare: TCG, World Boss Fight. Arena: solo pelle (ArenaCovo.css), mai la struttura.
- Tema unico: "R · Il Covo del Drago" (scuro; Fuoco/Gelo; admin Arcano). Pergamena e Nesso sono storia.
- Parallax: mai background-attachment:fixed (rotto iOS). Usa translateY/sticky.
- Lavora su UN pezzo alla volta, committa, poi fermati.
- Test di accettazione B&N: "prima" e "dopo" devono sembrare due siti diversi anche in bianco e nero.

## Workflow per ogni pagina
1. FASE 0 (no codice): mini-audit STRUTTURA VECCHIA → STRUTTURA NUOVA (chiaramente diversa) + alternative con raccomandazione + lista file. Fermati e aspetta conferma.
2. Implementa SOLO quella pagina. Niente commit/push se non richiesto (l'utente rivede dal locale).
3. Aspetta il giudizio B&N dell'utente, poi pagina successiva.

## STATO / SAVEPOINT (aggiornato 2026-06-14)

### FATTO
- **Tema chiaro "Pergamena Antica"** esteso a tutto il sito (commit Fasi 1–6).
  - Token: `src/styles/light-theme.css`. Sistema cine condiviso: `src/styles/cinematic.css`. Flottanti/layout: `src/styles/layout.css`. Caricati in `src/main.jsx` (ordine: style → theme → shell → light-theme → layout).
  - Pagine di gioco (Arena/TCG/World Boss/Pet) restano SCURE via `body.theme-dark` (toggle per-rotta in `src/App.jsx`).
- **Restyle STRUTTURALE — PILOTA = Home + shell/nav (COMPLETO):**
  - **STEP A — nav globale** (`src/App.jsx` componente `MobileBottomNav` + `src/styles/layout.css`):
    - Mobile/tablet (≤1300px): **bottom-bar** `Home · Mondo · Eroi · Gilda · Menu`; Mondo/Eroi/Gilda aprono **bottom-sheet** a griglia; **Menu** apre il drawer completo (Agent, Update, Battaglia, DM Tools, DM Admin…). Burger nascosto su mobile.
    - Flottanti riordinati SOPRA la barra: chat (dx), presenza online (sx), calendario Home (sx, impilato). Safe-area iOS ovunque.
    - Desktop (>1300px): top-bar con nav inline invariata (copertura identica).
    - La bottom-bar è un `<div role="navigation">` (NON `<nav>`, altrimenti eredita lo stile drawer di shell.css). Z-index 1065 (sopra il backdrop sheet).
  - **STEP B — contenuto Home** (`src/pages/Home.jsx` + `src/pages/Home.css`, nuovo componente `PantheonGrid`):
    - Hero **asimmetrico**: immagine full-bleed dx + placca-pergamena sx, titolo a sinistra, CTA in colonna→riga, parallax translateY, scroll-cue ad anello.
    - Lore **manoscritto**: desktop = marginalia sticky (Capitolo I) + articolo con rubrica/illustrazione float/capolettera; mobile = colonna unica. Prosa originale invariata.
    - Pantheon: **fascia etichettata** + **griglia carte** (1/2/3 col) con **dettaglio al tap (modale)**. Sostituisce il vecchio accordion.

### DA FARE — RESTYLE COMPLETATO ✅ (2026-06-14)
Tutte le pagine del giro sono state ristrutturate (changelog v25/v26/v27). Sintesi:
1. Eroi / Party → "Registro Araldico" (hero asimmetrico, indice sigilli, casate a doppia pagina, scheda eroe modale). ✅
2. NPC → "Schedario dei Volti" (capitoli per città a marginalia, schede-dossier orizzontali). ✅
3. Geo → "Atlante Geomantico" (indice continenti inline; **bug "Scorri" + iconcine in riquadro bianco RISOLTI**). ✅
4. Mercato → "Banco del Contrabbando" (hero + rango Ratto come sigillo + rubrica; loot-card/aste intatte) + ItemDetail "cartiglio di stima". ✅
5. Bacheca → "Albo degli Incarichi" (albo incorniciato + missive appuntate; animazione apri-pergamena intatta). ✅
6. Riassunti → "Codice delle Memorie" (gruppi a doppia pagina con marginalia + export PDF). ✅
7. Crafting → "Tomo dell'Artigiano" (sommario a capitoli + rubriche di capitolo numerate). ✅
8. Gilda dei Ratti → "Codice del Sottosuolo" (lore manoscritto + gradi a scala gerarchica). ✅
9. Cinema → hero a locandina + rubrica (teatro/featured/sala invariati). ✅
10. Feedback → hero asimmetrico (form/dashboard invariati). ✅
11. QuestDetail → hero "missiva" con copertina dinamica. ✅
12. Notifications → hero asimmetrico (card invariate). ✅
13. Bottega Arena → hero + rubrica Potenziamenti (shop/classi/master invariati). ✅
14. Assistente → masthead "oracolo" (chat invariata). ✅
15. Scheda PG → palette ribaltata da grimorio scuro a Pergamena chiara. ✅
16. Pannelli DM/Admin → masthead condiviso a filetto (`GeneraNPC.css`); `admin.css` già a tema. ✅
17. WorldMap → titolo a cartiglio cartografico, mappa/zoom intatti. ✅
18. Updates → eyebrow/rubrica (già a tema). ✅

### Note / aperti
- `pgSheetEditor.css` (editor PG del master) e i restanti pannelli admin minori: già su `admin.css` chiaro; eventuale rifinitura strutturale dedicata se richiesta.
- Rail `cine-side-nav`: dismesso sulle pagine ristrutturate a favore di indici inline / marginalia.
- `Antico_pantheon.png` non più mostrato nella sezione Antichi (eventuale reinserimento nella fascia).

### Verifica
- Build: `npx vite build`. Dev: `npx vite --port 5188`. Test mobile 414px + desktop 1568px. Applica il test B&N a ogni pagina.
